/**
 * server/index.ts
 *
 * This app uses Supabase BaaS for all backend logic (auth, database, RLS, RPC,
 * Edge Functions). There is no custom Express server required.
 *
 * To add server-side functionality beyond Supabase:
 *  - Add Node/Express routes here
 *  - Update package.json with a "server" script
 *
 * Supabase Edge Functions live in supabase/functions/
 * Database migrations live in supabase/migrations/
 */

export {};
